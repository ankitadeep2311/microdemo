apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: microservices-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
        - name: catalog-service
          image: microdemo/catalog-service:1.0.0
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server.microservices-demo.svc.cluster.local:8888"
            - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
              value: "http://zipkin.monitoring.svc.cluster.local:9411/api/v2/spans"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: microservices-demo
spec:
  selector:
    app: catalog-service
  ports:
    - port: 8083
      targetPort: 8083
